services:
  spinner-api:
    build:
      context: .
      dockerfile: Dockerfile
      target: build-api
    ports:
      - "8080:8080"
    environment:
      - SERVER_HOST=0.0.0.0
    env_file:
      - .env
