info:
  name: Check API health
  type: http
  seq: 1
  tags:
    - health healthz

http:
  method: GET
  url: "{{baseUrl}}/healthz"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/healthz"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
