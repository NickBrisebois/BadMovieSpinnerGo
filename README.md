# Bad Movie Spinner (Go Edition)

Largely Go-based rewrite of my BadMovieSpinner originally done in Typescript

## Debugging

Run:

```sh
make dev
```

and then run your debugger to attach to `127.0.0.1:2345` (see `.zed/debug.json`)
